# nestesed-form
# Nested Form Task
A dynamic form built with React that allows users to add questions and nested sub-questions dynamically.

## Setup and Run Instructions
1. Clone the repository
2. Run `npm install`
3. Run `npm run dev`
4. Open the local host link in your browser.
